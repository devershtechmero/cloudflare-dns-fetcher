import { Layout } from "@/components/Layout";
import LoginForm from "@/components/LoginForm";

const Index = () => {
  return (
    <Layout>
      <LoginForm />
    </Layout>
  );
};

export default Index;
